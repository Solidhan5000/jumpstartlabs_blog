<!DOCTYPE html>
<html>
<head>
  <title> <%= yield(full_title(:title)) %> </title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div id="main_wrapper">
    <p class="flash"><%= flash.notice %></p>
    <%= yield %>
    <div id="debug">
      <%= debug(params) %>
    </div>
  </div>

</body>
</html>
