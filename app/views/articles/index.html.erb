<% provide(:title, 'Hasan Main Blog Page') %>

<h1>All articles on page</h1>

<ul id="articles">
  <% @articles.each do |a| %>
    <li>
      <%= link_to a.title, article_path(a) %>
    </li>
  <% end %>
</ul>

<%= link_to 'Create new article', new_article_path %>